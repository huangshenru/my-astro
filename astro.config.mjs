// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://huangshenru.github.io',      // 你的 GitHub Pages 根地址
  base: '/my-astro',                           // 仓库名，必须带斜杠
  // ... 其他配置
})